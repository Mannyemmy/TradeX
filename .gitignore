/node_modules
/public/hot
/public/storage
/storage/*.key
/vendor
.env
.env.*
!.env.example
.env.backup
.phpunit.result.cache
docker-compose.override.yml
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/.idea
/.vscode
/.claude

# Server-owned config - edited directly on the server, never deploy from local.
# These are excluded in .cpanel.yml too, so a deploy cannot overwrite them.
# Performance rules that belong in the live .htaccess live in
# server-config/htaccess-performance.conf (tracked, for reference only).
.htaccess
web.config
